# Never put this key in browser JavaScript.
# Keep it on the server as an environment variable.
OPENAI_API_KEY=sk-your-key-here

# Optional. Defaults to gpt-5 in server.js.
OPENAI_MODEL=gpt-5

PORT=3000
